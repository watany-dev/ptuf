# ptuf

Deterministic guardrail CLI for coding agents.

```bash
npm install -g ptuf
ptuf --version
```

This package is a small JavaScript launcher plus one optional native binary
package for your platform. It does not run install scripts.

See <https://github.com/watany-dev/ptuf> for documentation.
