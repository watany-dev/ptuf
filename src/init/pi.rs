//! `ptuf init pi` — install ptuf as a Pi Coding Agent extension.

/// Pi-specific `ptuf init pi` flags. Populated by the CLI parser in M5.
#[derive(Clone, Debug, Default, PartialEq, Eq)]
pub struct PiInitOptions {}
